import { DetailanimesPipe } from './detailanimes.pipe';

describe('DetailanimesPipe', () => {
  it('create an instance', () => {
    const pipe = new DetailanimesPipe();
    expect(pipe).toBeTruthy();
  });
});
