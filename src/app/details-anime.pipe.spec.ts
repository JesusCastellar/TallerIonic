import { DetailsAnimePipe } from './details-anime.pipe';

describe('DetailsAnimePipe', () => {
  it('create an instance', () => {
    const pipe = new DetailsAnimePipe();
    expect(pipe).toBeTruthy();
  });
});
